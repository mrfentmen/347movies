# 347movies — Tasks

The complete task list for building 347movies to production readiness. Work is phase-ordered per `specs.md` and governed by `constitution.md`. Every task has an acceptance criterion requiring **raw proof** — real command output, test results, live checks. No task is marked done on a claim.

**Status legend:** `[x]` done with raw proof · `[~]` partially done / blocked on external prerequisite · `[ ]` not started. Verification happened against the live archive.org APIs with a local `wrangler pages dev` server (2026-08-15 build session) and, after credentials were provided, **on the production URL https://347movies.pages.dev**. Remaining blockers: the deployed API token is Pages-scoped only (no Workers KV, no zone/WAF access).

---

## Phase 1 — Foundation

- [x] **T1.1 Scaffold the repository.** `package.json` (minimal pinned dev deps: wrangler 4.123.0, typescript, @cloudflare/workers-types, @types/node), `wrangler.jsonc` (Pages project, `MOVIES_KV` binding, `SITE_URL`), `.gitignore` (node_modules/.env/secrets), tsconfigs, `README.md`. *Evidence: `npm install` added 43 packages with 0 vulnerabilities; secret/tree sweep clean (no `.env`, no client-side secrets). Note: the parent workspace repo ignores this folder (its `.gitignore` `/*` rule); the folder is untracked — documented in `changelog.md`.*
- [x] **T1.2 Create the Cloudflare Pages project.** `wrangler pages project create 347movies --production-branch main` (success) and `wrangler pages deploy public --branch main --project-name 347movies` (success, 3 deploys). *Evidence: deploy logs captured in changelog; live URL https://347movies.pages.dev serves the site.*
- [x] **T1.3 Security headers.** `_headers` + `functions/_middleware.ts` set CSP (self + `frame-src https://archive.org`, no inline scripts/styles), HSTS (+preload), nosniff, `X-Frame-Options: SAMEORIGIN`, `Referrer-Policy`, `Permissions-Policy` on every page and API response. *Evidence: `curl -I` on `/` and API responses shows all headers; player embed still loads under CSP; console clean.*
- [x] **T1.4 Worker stub + health route.** `GET /api/health` → `{"ok":true,"service":"347movies",...}` with `Cache-Control: public, max-age=60`. *Evidence: live curl returned 200 JSON on the local dev server.*
- [~] **T1.5 WAF and edge settings.** Cloudflare WAF, bot fight mode, forced TLS on the zone. *BLOCKED: requires Cloudflare account/zone access. Not faked — marked unverified until applied.*

## Phase 2 — Catalog

- [x] **T2.1 Archive.org search integration.** `lib/archive.ts` calls `advancedsearch.php` filtered to the curated film collections `feature_films OR prelinger OR moviesandfilms` + `mediatype:movies` plus a declared license (`licenseurl` CC/PD mark), with bounded rows/pages and fl-field selection. (Expanded 2026-08-15 follow-up: union ≈ 18,489 legal-marked films, measured live; `classic_films`/`SilentEra`/`publicdomainmovies` carry zero license marks and are excluded. 2026-08-15 second follow-up: `films=1` now also excludes trailer titles; third follow-up adds serial-installment tokens — the clause is `-title:(episode* OR season* OR pilot OR "ep." OR trailer* OR chapter OR part)` — applied server-side to search/browse/home/random so no catalog view presents a trailer or serial installment as a film; drops ~2,561 non-film titles from the 18,488 legal union → **15,927 films**, Solr-identical. 2026-08-16 fourth follow-up: adds `teaser*` and `"music video"` — the clause is now `-title:(episode* OR season* OR pilot OR "ep." OR trailer* OR teaser* OR "music video" OR chapter OR part)` — the observed live bug (a music video reached `/api/random`'s fallback) and a catalog scan showed teasers/music videos slipped the policy; re-verified live: **15,917 = 15,917 kept-sets, identifier-identical**, the 10 new drops individually audited (9 non-films; `Teaserama` is the accepted one-film loss — its title starts with "teaser", Solr's `teaser*` forces the equivalence, still reachable by direct URL).) *Evidence: `/api/search?q=nosferatu` returned 19 real films (was 12 pre-expansion), all `license: "publicdomain"`, from the live API; catalog probe: 18,489 legal-marked films. Unit coverage (2026-08-15): `tests/archive-unit.test.ts` — 13 mocked-fetch tests for the clause assembly (legal base clause always pinned, films-only opt-in, query/subject/decade clauses, rows/page/fl[]/sort), response parsing + fail-closed bad-shape, and all four fetchers (metadata incl. is_dark + 404-no-retry, sitemap pairs + blank-id skip, index docs, search-doc fallback found/null); retry policy separately covered in `tests/retry.test.ts`.*
- [x] **T2.2 Archive.org metadata integration.** `metadata/<identifier>` normalized into the typed record (title, year, description, genres, creators, subjects, thumbnails, runtime, license, source_url). *Evidence: `/api/movie/it-1927` returned the complete record (year 1927, license publicdomain, hasVideo true, creators parsed). Note: `night_of_the_living_dead` is `is_dark: true` (removed) on archive.org, so verification used **It (1927)**; NOTLD correctly fails closed → 404.*
- [~] **T2.3 KV cache.** Logic verified locally with KV emulation (cold 0.743 s vs warm 0.0045 s). **Production KV is blocked**: the deployed token is Pages-scoped (auth error 10000 on KV create/list). A Cloudflare-native **edge response cache** (`lib/edge-cache.ts`, Cache API) is active instead — verified live `cf-cache-status: HIT`, local cold→warm 0.65 s→0.004 s. KV upgrade path documented in `wrangler.jsonc`/`README.md` (needs a KV-scoped token).
- [x] **T2.4 Input validation on all params.** Query ≤ 80 chars + sanitized (no Solr/URL/traversal chars), identifier `^[A-Za-z0-9._-]{1,120}$`, page 1–100, genre/decade/sort whitelists. *Evidence: fuzz battery — empty/huge queries, `..%2F..%2Fetc%2Fpasswd` identifiers and queries, `a b<c>`, page 9999/-1 all return 400 JSON, never a crash or upstream call (traversal-in-query 502 was fixed by stripping slashes, re-verified 200 with sanitized text).* 
- [x] **T2.5 Rate limiting.** Per-IP 60 req/min on every dynamic upstream-capable route: `/api/*`, SSR `/movie/*`, `/sitemap.xml` (added in the hardening pass); static CDN assets are never throttled. *Evidence: `/api` burst 58×200 then 7×429; `/movie/` burst 58×200 then 12×429 with `/sitemap.xml` → 429 in the same window and `/` → 200 (unthrottled); `isRateLimitedPath` unit-tested.*

## Phase 3 — Pages

- [x] **T3.1 Home page.** Featured/recent sections, genre chips, search, all rendering from `/api` with real posters. *Evidence: browser snapshot shows real films in all three sections (film noir: The Killing, Shadow of a Doubt; silents: Faust, Potemkin, Pandora's Box); console clean.*
- [x] **T3.2 Search + browse.** Full-text search (live archive.org relevance, license-gated) and genre/decade/sort browse served from the **local catalog index** (`lib/catalog-index.ts` — the full ~18.5k-film catalog built once per 24h, edge-cached; zero upstream calls per browse request; the **whole catalog is pageable**, the old 100-page/2,400-film cap is gone). The `films=1` exclusion (episodes, trailers, teasers, music videos, serial chapters/parts) is replicated token-for-token from the Solr clause — verified live 2026-08-15 (15,927 = 15,927) and re-verified 2026-08-16 after the teaser/music-video additions (**15,917 = 15,917, identifier-identical**), including the Solr-tokenizer edge cases (bare `ep` token matches Solr's `"ep."` phrase; "Pilot's" stays because Solr keeps apostrophes attached; plural "Chapters" compilations stay while "Chapter 10" installments drop; the adjacent "music video" token pair matches Solr's phrase across hyphen boundaries). Search applies the same policy server-side. *Evidence: `/api/browse?genre=film-noir` total **177 = Solr 177 exact** (verified by live diff against `subject:("film noir")`), `/api/browse?decade=1920&sort=oldest` (621, oldest first, first year 1920), `/api/browse?films=1&page=500` (page 500/664 serves 24 results), `/api/browse?films=1&page=9999` → 400, `/api/search?q=noir` (396, top hits all films — "Nightmare Alley trailer" gone), `/api/search?q=detour` (33 — "Detour (1945)" now #1, serial chapters gone), `/api/random` sampled landings all inside the films-only set.*
- [x] **T3.3 Movie detail + embedded player.** SSR `/movie/<identifier>` with poster, synopsis, metadata chips, and the `https://archive.org/embed/<identifier>` iframe. *Evidence: browser network log `GET https://archive.org/embed/it-1927 → 200 (Document)`; screenshot shows the film's opening credits playing in the embed; page console clean; ad-free (no ad containers over/inside the player).*
- [x] **T3.4 Static pages.** About, privacy, terms, styled 404. *Evidence: all load with no console errors; `/definitely-not-a-page` returns 404 with the styled page; noindex on 404 via `_headers`.*
- [x] **T3.5 Mobile-first layout.** Mobile-first CSS (2→3→4→6-column grids, sidebar collapse at 900px, player full-width 16:9). *Evidence: Lighthouse runs against the live site — accessibility 100 on every page type; no horizontal overflow measured down to 477px (deploy #34) and the 320px header-search edge case fixed; breakpoints verified by inspection, desktop rendering, and the Lighthouse mobile-emulation runs.*

## Phase 4 — Monetization

- [~] **T4.1 Ad slots.** Sidebar + leaderboard slots in clearly marked markup (`data-ad-slot`, labeled "Advertisement"), never over/inside the player, now with a real advertiser contact line (`contactae2000@gmail.com`) and an `/about#advertise` section + footer link. *Slots verified live on home/search/browse/movie pages (email present in served HTML). Real ad network code NOT wired because no network is configured — per constitution §4/§12, no fake ad rendering; rendering is pending a real network contract.*
- [~] **T4.2 Affiliate links.** Mechanism implemented and unit-tested (Amazon search URL with env-bound tag, visible disclosure, `rel="sponsored noopener"`), shown only for films that are NOT freely watchable. *Because every catalog film is legally free, no catalog film ever renders the link — the mechanism is real (tests pass) but live rendering is not demonstrable with the current catalog by design (vow 8: free comes first).*
- [x] **T4.3 Ad loader mechanism (BUILT — deploy #45 = dc5a07af, decision 001).** `lib/ad.ts` — `adConfig(env)` validates `AD_NETWORK_SCRIPT` (https-only, host on the empty-until-chosen allowlist constant); `/api/ad-config` serves the config gate (edge-cached 300s, fail-closed, HEAD parity); the client bootstrap in `public/js/app.js` fetches the gate on every page view and injects the network's `<script async>` into `<head>` only when enabled, fail-closing on any error (network/429/disabled/malformed — the reserved slot note stays, no third-party script). *The council's "no endpoints needed" position was refined with justification: the leaderboard slots live on STATIC pages (public/*.html) that cannot read env vars, so the uniform seam is one tiny edge-cached config endpoint; the script URL is public configuration, not a secret. The hard timeout is structural: async injection never blocks parsing and a failed/hanging network leaves the note in place — a removal watchdog was rejected (risk of layout shift, zero benefit). Unit-tested 5 paths (off / on / bad URL / non-allowlisted / dormant-by-structure). Smoke guards prove `enabled:false` + zero third-party scripts on every slot page while unconfigured (constitution §12). Dormant until a host joins the allowlist (T4.5).*
- [x] **T4.4 Ad privacy + network contract (BUILT — deploy #48, decision 001).** `public/privacy.html` gains the standing "Advertising — the standing disclosure" section (constitution §5: ads only in marked slots, never on the player; the loader's one self-origin `/api/ad-config` request; no data collected or passed to the network; the network is named + its policy linked here BEFORE any ad renders). FOUNDER-CHECKLIST item 4 becomes the acceptance checklist: legal-only compatible, sidebar/leaderboard placement only (no player-format ads), no auto-playing audio, brand-safety controls, CSP-compatible tag (no unsafe-inline; exact script host for the allowlist), documented privacy disclosure. Smoke guard: privacy page carries the standing disclosure.
- [ ] **T4.5 Ad verification + enablement (PLANNED — decision 001).** Smoke guards proving zero third-party scripts on any page while unconfigured; then — only when a real network is chosen — the reviewed CSP allowlist diff (`script-src` gains the exact network host(s); `frame-src` stays archive.org-only) plus the env var and live browser verification (ads in exactly the two slot types, player untouched, Lighthouse a11y 100 + CLS 0 unchanged).

## Phase 5 — Hardening

- [x] **T5.1 CSP audit.** CSP allows only self + `frame-src https://archive.org`; no `unsafe-inline`/`unsafe-eval`; no inline executable scripts or styles anywhere (static pages use external JS/CSS only; the only inline `<script>` on SSR movie pages is a data-only `type="application/ld+json"` block, which browsers never execute and which is exempt from `script-src` — added for structured data, see T6.1; verified parseable and console-clean in-browser). *Evidence: headers verified; player still loads; JSON-LD block parses with zero console errors.*
- [x] **T5.2 Error handling sweep.** API routes return structured JSON errors (400/404/429/500/502); front end renders friendly fallbacks with retry; no uncaught exceptions. *Evidence: all edge-case responses verified; home error path renders `.error-box` with "Try again" (retry wired).*
- [x] **T5.3 Test suite.** 55 tests: validation fuzz (incl. `validateFlag`), normalization, cache logic, rate limiter (incl. `isRateLimitedPath`), affiliate, movie-page layout paths (player / no-video / unavailable / honest upstream-502, JSON-LD honest-emission incl. the no-fabrication case), archive-client retry policy (5xx / network-error / 4xx / fail-closed), plus live archive.org integration tests marked `[integration]` (incl. a filmsOnly episode-exclusion test). *Evidence: `npm test` → 55 pass, 0 fail.*
- [x] **T5.4 Dependency audit.** *Evidence: `npm audit` → 0 vulnerabilities; deps pinned exact.*
- [x] **T5.5 Back-door sweep.** *Evidence: grep sweep for debug/admin routes, secrets, TODO/stub/placeholder code, `.env` files — clean (only legitimate documentation mentions).*

## Phase 6 — SEO & polish

- [x] **T6.45 Security review (read-only, no deploy).**  security-review skill pass over
  the full attacker-controlled surface — every route, the middleware, the client bundle,
  and the shared libs — with end-to-end data-flow tracing and live exploitability probes.
  **No high-confidence vulnerabilities identified** (0 findings): hostile identifiers →
  400 with no upstream call, XSS-shaped queries sanitized, Solr-injection contained to a
  controlled 502, `<script>` params → 400, security headers present, no inline scripts on
  the SSR page, no secrets (`.env` gitignored). Added the health battery's security
  re-review staleness gate (`scripts/security-review-due.sh`, default 90-day cadence,
  `SECURITY_REVIEW_MAX_AGE_DAYS` to tighten) so a scheduled run flags a stale review.
  Report: `docs/security-review-2026-08-16.md`. *Evidence: live probes against the dev
  server, npm audit 0 on both trees, typecheck clean, 128/128.*
- [x] **T6.44 Site-architecture pass — fix the /genre orphan (deploy #77 = 8259a33).**
  The `/genre` Film Noir showcase (hero + live grid, sitemapped and indexed) was linked
  from nowhere — its only inbound link was from its own page; the T6.10 audit's "no
  orphans" claim missed it (the page post-dates that audit, and the smoke suite checked
  its structure/canonical/id-scan/no-auth without ever asserting an inbound link). Home
  Film Noir pill now → `/genre` (hub-and-spoke: the page cross-links back to
  `/browse?genre=film-noir`); smoke guard asserts the link (orphan rule, smoke 232 →
  233); `docs/site-architecture.md` rewritten to the full IA deliverable set (hierarchy,
  Mermaid sitemap, URL map table, nav spec, linking audit). *Follow-up (deploy #78 =
  9658bc2): `aria-current="page"` on the active Film Noir chip (WCAG 1.3.1, matches the
  header nav). Evidence: typecheck clean, 128/128, dev smoke 233/233, canonical smoke
  233/233, both deploys verified `environment = production`.*
- [x] **T6.1 Meta/OG tags + structured data.** Per-film title/description, `og:type=video.movie`, `og:image` (real archive poster), canonical URLs on the SSR film page, plus a schema.org **`@graph` JSON-LD data block** — VideoObject (name, description, thumbnailUrl, embedUrl = the real archive.org embed, uploadDate = the real archive.org added date, duration from the real runtime when available — never fabricated) and BreadcrumbList (Home → film, real canonical URLs). The homepage separately carries WebSite + SearchAction (sitelinks searchbox). *Evidence: verified in the served HTML for `/movie/it-1927` (`@graph` parses to BreadcrumbList + VideoObject, `uploadDate=2025-07-27`); browser console clean. Shared-link preview itself not testable without a live URL.*
- [x] **T6.2 sitemap.xml + robots.txt.** Dynamic `/sitemap.xml` (6 static pages + the full legal catalog — 18,488 films) built from the **local catalog index** (one no-page rows=50000 request per 24h; the rows=1000 paged fallback + per-page retry + identifier dedupe now lives in the index builder) and `robots.txt` with sitemap reference. *Evidence: live `/sitemap.xml` serves **18,494 `<url>` entries** from the index in ~0.06s (was a 10.3s build), 18,488 `<lastmod>` entries; robots.txt served. Search Console fetch requires the deployed domain — pending.*
- [x] **T6.3 Performance pass.** Static assets edge-cached (`Cache-Control: public, max-age=3600`, etag revalidation), no render-blocking junk, image dimensions set everywhere (poster 600×800, cards 300×400), `fetchpriority="high"` on the movie-page poster (LCP 1787→535ms), mobile-first CSS, poster `object-fit: cover` (never stretches artless frame grabs), skeleton grids reserve the browse/search results space. *Evidence: Lighthouse against the live site — performance: home 100, about 100, movie 99-100, browse 67→98, search 76→100; CLS 0 on home/movie/about/browse and 0.021 on search (the 0.72 browse/search CLS from JS-populated grids was fixed by skeleton grids, deploys #35-#36); best-practices ≥ 92 everywhere (only archive.org's own third-party iframe cookies remain); accessibility 100 everywhere; SEO 66 on browse/search is the deliberate `noindex` on dynamic pages.*
- [x] **T6.4 Perf pass 2 (deploys #45–#47 = 5df5ccfa → 7b7f4449 → a54f1777).** `<link rel=preconnect href="https://archive.org">` on every page head (poster LCP + player connection — was silently blocked by `connect-src 'self'`, now deliberately relaxed to `'self' https://archive.org https://*.archive.org` with a documented rationale; the page's own JS never fetches archive.org, so this enables only the connection hint) and cross-document View Transitions (`@view-transition { navigation: auto }`, CSS-only, zero risk). *Speculation Rules (prerender) were attempted TWICE and honestly removed: the external `speculationrules.json` form isn't supported by Chrome (logs "External speculation rules are not yet supported", verified by Lighthouse), and the inline form is blocked by the strict CSP even with a byte-exact `sha256-` hash — Chrome's CSP error names the exact digest yet still refuses (Chrome doesn't honor hash/nonce allowlisting for speculation rules; only `unsafe-inline` would work, which this site's security posture forbids and the smoke suite guards). Removed entirely per "leave no errors behind" — the smoke suite now asserts pages carry NO speculation rules. Lighthouse after (cache-busted, console-clean): home 99/100/100/100 (FCP 1.3s, LCP 1.9s, CLS 0, TBT 0); movie 75-99/100/96/100 — best-practices 96 is only archive.org's own iframe-cookie flag, and the LCP spread is archive.org image-server latency (external; preconnect helps when they're fast). Smoke 90/90.*

## Phase 7 — Production launch

- [x] **T7.1 Final deploy.** *Evidence: `wrangler pages deploy public --branch main --project-name 347movies` succeeded; production URL https://347movies.pages.dev serves the site (deploy logs in changelog).*
- [x] **T7.2 End-to-end live verification.** *Evidence: live walkthrough — all routes 200 (home, search, browse, movie, about, privacy, terms, sitemap, robots, health), player embed present on `/movie/it-1927`, security headers verified on live responses, 404 returns 404, edge cache `cf-cache-status: HIT`, live edge cases (empty query, traversal, page 9999, dark item) behave per spec. Browser-level rendering verified locally against identical code (screenshots + clean console).*
- [x] **T6.5 Constitution ↔ smoke guard audit.** `docs/guard-audit.md` maps every constitution rule (1–12) and vow (1–11) to the live smoke check that guards it; all re-read and checked against the deployed site — no violations. *The audit found one real gap and closed it: §7/vow 4 ($0 storage) is enforced by `media-src https://archive.org` in the CSP but had no smoke guard — added; smoke 91 → 92. Evidence: guard-audit.md, dev+canonical smoke 92/92.*
- [x] **T6.6 WCAG guard pass.** `lang="en"` (WCAG 3.1.1) folded into every page-structure check, plus two new SSR guards on the movie page: the player iframe's `title` (WCAG 4.1.2) and the poster's `alt` (WCAG 1.1.1). *The fundamentals were verified by hand and Lighthouse (a11y 100 everywhere) but only two were CI-guarded; now the whole set is. Evidence: dev+canonical smoke 94/94.*
- [x] **T6.7 Canonical site-URL resolver (deploy #49 = 0acc3524).** `lib/site-url.ts` — `resolveSiteUrl(request, env)`: `SITE_URL` env override wins (the deliberate SEO pin from `wrangler.jsonc` vars — one canonical host), request-host resolution as the fallback for environments without the binding (custom-domain ready, https-forced, localhost keeps http, hostile hosts rejected), pages.dev default last. Refactored `sitemap.xml`/`movie`/`api/random` to use it; 4 unit tests. *Investigation found the real design: `SITE_URL` is pinned in `wrangler.jsonc`, so the override always wins in production (correct for SEO); the first guard I wrote (canonical=request host) was wrong for that design and now asserts canonical=pinned origin + right path + og:url match. FOUNDER-CHECKLIST documents the one-line `SITE_URL` update when a custom domain is attached. Evidence: dev+canonical smoke 96/96, live canonical verified, deploy verified production.*
- [x] **T6.8 Security threat-model pass (deploy #52 = f63dea32).** STRIDE over the trust boundaries (HTTP input, archive.org third-party metadata, client DOM) found a stored-XSS class: archive.org `year` rendered unescaped in the SSR year chip, the year suffix, and the client `movieCard` (home/browse/search cards) — `watchCardHtml` escaped it, `movieCard` didn't. Fixed all three with the shared `escapeHtml`; regression test (hostile year → zero raw occurrences, escaped form present, no executable `onerror` img). *Also: `npm audit` 0 vulnerabilities (zero runtime deps), secrets sweep clean, identifier charset validated before any upstream call (no SSRF). Evidence: 114/114 tests, dev+canonical smoke 96/96, live year chip renders normally, hostile-year behavioral check through the real render path.*
- [x] **T6.9 API→DOM audit + ad-boundary hardening (deploy #53 = ed27ba59).** Mapped every browse/search API field to every client render path — client renders only title/year/thumb/id, all escaped; SSR escapes everything. Ad bootstrap now independently requires https before injecting (defense in depth on the remaining external-integration boundary; server allowlist was already airtight). *Both fixes CI-enforced: smoke 96 → 98 (deployed app.js must carry `escapeHtml(String(m.year))` and the https check). Evidence: 114/114 tests, dev+canonical smoke 98/98, deploy verified production.*
- [x] **T6.10 Site-architecture pass (deploy #54 = e0e1790b).** IA audit of the live site (hierarchy, URLs, nav, orphans, internal linking) → flat two-level structure is correct for the site type; no orphans; URLs clean. *Fixed the one gap: visible breadcrumbs (`Home / <Title>`, aria-current) now mirror the JSON-LD BreadcrumbList on the movie page family — the UI and structured data agree; regression-tested; audit in docs/site-architecture.md. Evidence: 115/115 tests, dev+canonical smoke 98/98, breadcrumb visually verified.*
- [x] **T6.11 Web design review (no deploy).** Visual inspection of all 7 page types at a sub-tablet viewport with an automated layout audit (overflow, horizontal scroll, broken images, text clipping, console) — **zero issues**; mobile-first CSS verified structurally sound (flex-wrap header, 2→3→4→6 grid, 320px-verified input cap). *Evidence: docs/design-review.md, dev+canonical smoke 98/98.*
- [x] **T6.12 Real-browser E2E suite (no deploy).** Playwright (system Chrome, no browser download) against the dev server: full user flows — search → movie → save → watchlist → unsave → browse genre filter → Surprise me → mobile 375px (the viewport the design review couldn't reach) — plus console/page-error gates filtered to our origin. **24/24, zero console issues from our origin, zero page errors** (all third-party noise from the archive.org embed/thumbnail CDN/ad-block, documented in the suite). *The investigation was worth it: the surprising finding was NOT a site bug — Playwright's actionability sampler never completes on this site in this headless environment (rAF fires, boxes stable, zero mutations; the stall is inside Playwright's injected stability machinery), so the suite clicks via raw protocol-level mouse events with elementFromPoint verification; a genuine harness trap (off-viewport elementFromPoint = null → below-fold clicks needed a JS scrollIntoView, since Playwright's scroll uses the same broken sampler). Evidence: `npm run test:e2e` (scripts/e2e_test.py), run twice green.*
- [x] **T6.13 Web-interface-guidelines pass (deploy #55 = 00d3e153).** Audited all UI code against the Vercel web-interface-guidelines rule set — the site already passed the majority (labels, :focus-visible, reduced-motion, explicit transitions, image dimensions, alt text, …/curly quotes, min-w-0, URL state, hover states, no user-scalable=no). *Fixed the six real gaps: (1) `color-scheme: dark` — the dark-only theme never told the UA, so native scrollbars/controls could render light; (2) `<meta name="theme-color">` (#0c0d11) on all 8 static pages + the SSR shell — mobile chrome no longer flashes white; (3) `touch-action: manipulation` on interactive elements — kills the double-tap zoom delay; (4) `-webkit-tap-highlight-color` set intentionally (accent-tinted) instead of the default gray flash; (5) safe-area insets on the full-bleed sticky header/footer for notched devices; (6) **Clear watchlist now confirms first** (destructive action, never immediate — one stray tap no longer erases the saved list). Plus `text-wrap: balance` on display faces (no widows) and `role="status"` on the browse/search result counts so async updates are announced. *All 11 new invariants CI-guarded: smoke 98 → 109. Evidence: 115/115 tests, dev+canonical smoke 109/109, E2E 24/24, deploy verified production, live theme-color/touch-action/confirm confirmed on the deployed bundle.*
- [x] **T6.18 SSR hostile guards + scheduled health battery + browser-battery hardening (no deploy).** (a) **SSR guards** (smoke 140 → 149, 9 checks): hostile /movie/* identifiers → 400, never echoed, intact HTML; negative-proven (escaping held even with the fail-closed 400 removed). (b) **Scheduled battery:** `npm run health` → `scripts/health-battery.sh` appends an honest dated pass/fail changelog entry; runbook gains launchd/cron wiring + the dev-only self-exhaustion note (suite ≈50 rate-limited requests vs the 60/min single-isolate dev limit — verified fresh isolate 149/149; production always 149/149). (c) **Browser-battery hardening:** E2E movie-landing now reload-once-retries on the unavailable variant (upstream transient), mirroring the site's fetchWithRetry; three consecutive green battery runs. *Evidence: typecheck clean, 118/118 tests, dev + canonical smoke 149/149, browser battery green ×3.*
- [x] **T6.46 Operational hardening: git-bound deploys, rollback runbook, token hygiene, single-flight index build, browser battery in CI (PR #3).** (a) **`deploy.ts` gates:** refuse dirty tree / HEAD≠origin/main (`DEPLOY_ALLOW_DIRTY=1` emergency override), pre-deploy secret scan (public/functions/lib/wrangler.jsonc), merged-commit CI status print, token from gitignored `.env`; `assertDeployableGit` + `scanDeployFilesForSecrets` unit-tested, refusal path demonstrated. (b) **Single-flight catalog index** — concurrent cold reads share one upstream build (3 concurrent → 1 call, tested). (c) **`docs/rollback-runbook.md`** — list/identify/rollback (dashboard + git revert + emergency), token rotation; read commands practiced (current prod cross-checked). (d) **Token → `.env`** (0600, gitignored). (e) **CI browser job** (E2E/keyboard/mobile/axe on Playwright) — restored the E2E functional battery to CI (the unified hygiene-battery matrix covers live/prototype surfaces; this job covers user flows, keyboard walk, mobile/zoom, axe). *Evidence: typecheck clean, tests green, dev smoke, battery E2E + keyboard + mobile + axe 0, deploy-gate refusal demonstrated; a deploy follows the merge per repo convention.*
- [x] **T6.43 Browser batteries, unified (no deploy).**  One shared harness
  (`scripts/battery_common.py`: fail-closed third-party attribution — console errors
  filtered by source URL, pageerrors by message, 5xx by URL; archive.org +
  `chrome-extension://` noise — plus the shared check/summary/exit contract and browser
  selection) powering two thin batteries: `scripts/live_surface_battery.py` (live site,
  24 checks: home/browse/search/genre/watchlist/movie structure + hygiene) and
  `scripts/prototype_smoke.py` (shadcn sandbox dist, 13 checks: favicon, grid, movie
  page, keyboard, mobile, hygiene). One orchestrator (`scripts/browser-battery.mjs`,
  `npm run test:live-surfaces` / `test:prototype`, folded into `test:browser`) boots the
  right server per target and tears it down; one matrix CI job `browser-batteries`
  (live, prototype) runs both on every PR/push with bundled chromium (`BATTERY_CI=1`),
  including a fresh `npm ci` in `ui-prototype` for the prototype leg. Unifies the prototype smoke and
  the live-surface battery (previously two scripts, two orchestrators, two CI jobs; the
  prototype battery filtered console noise by message text and missed
  `chrome-extension://` artifacts). Implements
  `docs/superpowers/plans/2026-08-16-live-surface-battery.md`. *Evidence: 24 + 13
  checks green locally, root typecheck clean, 128/128.*
- [x] **T6.41 Perf follow-up + Lighthouse verification (deploy #76 = 0e6e5305).**  (a) Poster preload in `<head>` gains `fetchpriority="high"` — completes Lighthouse's LCP-discovery checklist (poster request traced at `High` priority, up from `Low`); guard pins the full attribute. (b) Measured on production: warm performance 98, LCP 1.8–2.2s, TBT 0ms, CLS 0, a11y 100, best-practices 96 (known archive.org-iframe flag), SEO 100; LCP element = the poster; load delay 4–7ms proves the preload. (c) Edge probe: warm TTFB 80–90ms (HIT, 300s) vs cold 2.3s→12.4s (cache-miss-only tail). (d) Early Hints (103): already auto-enabled for pages.dev domains — Cloudflare synthesizes 103 from the preload Link headers; no code needed (custom domain would need the dashboard toggle). Reports in `docs/reports/`. *Evidence: Lighthouse on production (3 runs saved), curl probes, Cloudflare docs; typecheck clean, 128/128, canonical smoke 232/232, deploy verified `environment = production`.*
- [x] **T6.40 Performance pass — movie-page LCP chain + font preload (deploy #75 = 6151193a).**  (a) Movie page: the player iframe (above-the-fold LCP element) was `loading="lazy"` (deferred fetch — backwards); now eager with `fetchpriority="high"`; the poster (LCP candidate) is preloaded in `<head>` via new `PageMeta.preloadImage` (fetch starts at parse time); card posters got `decoding="async"`. (b) Font gap: CSS uses Plex Mono 500 (eyebrows/chips) but only the 400 file was preloaded — `plex-mono-500.woff2` now preloaded in the SSR shell and all 9 static pages. +5 smoke guards (227 → 232). *Evidence: verified in a real browser (iframe eager + fetchpriority, poster preload in DOM, network log shows mono-500 fetched + poster fetched at parse), typecheck clean, 128/128, canonical smoke 232/232, deploy verified `environment = production`.*
- [x] **T6.39 Copy pass round two + motion-audit invariant (deploy #74 = da55cf2f).**  (a) Direction copy on the remaining quiet surfaces: browse empty-filter view ("try different filters or a different sort"), home feeds ("Nothing here yet — check back soon."), import failures (say what's wrong + where a valid file comes from), noscript fallbacks on all five pages (enable JavaScript + archive.org for no-JS visitors; watchlist keeps JS-focused direction). No smoke-guarded strings changed; +2 guards. (b) Motion audit: enumerated the motion inventory (lamp-strike, marquee-strike, card-hover transition), confirmed both animations killed under `prefers-reduced-motion` (transition by the universal rule), and added a smoke invariant that fails if a new `animation:` ships without a kill-list entry — verified it passes the real CSS and catches an un-killed animation on a new selector. *Evidence: typecheck clean, 128/128, canonical smoke 227/227, deploy verified `environment = production`.*
- [x] **T6.38 404 marquee strike + empty-state copy pass (deploy #73 = 41d50e2a).**  (a) `@keyframes marquee-strike` on `.notfound h1` (2.2s: catches, dips twice, settles — a tired neon sign), killed under `prefers-reduced-motion`. (b) Copy pass per frontend-design writing guidance: search no-results now offers the next step with a TV-aware browse link; empty watchlist is directive ("Browse the catalog and save a film") matching the Save vocabulary; no smoke-guarded strings changed. +3 smoke guards (220 → 223). *Evidence: computed style in browser (marquee-strike, 2.2s), canonical smoke 223/223, production app.js/css carry the changes, deploy verified `environment = production`.*
- [x] **T6.37 The hero lamp strike (deploy #72 = daa65f6f).**  Frontend-design pass on the established DESIGN.md brief: the hero beam (the signature projection-booth light) was static — now it strikes on load (1.6s: fade in, one dip at 72%, steady), CSS-only via `@keyframes lamp-strike` on `.hero::before`, disabled under `prefers-reduced-motion` (the beam is simply on). No token/layout/copy changes; DESIGN.md motion section updated (never-drift rule). *Evidence: computed style in browser (lamp-strike applied, 1.6s), canonical smoke 220/220, production CSS carries the keyframes, deploy verified  `environment = production`.*
- [x] **T6.36 Shared CatalogFilter type + client-seam design note (deploy #71 = 4f5a6f12).**  (a) `CatalogFilter` (lib/archive.ts) — the 5 fields shared by both catalog backends (`variant`, `genreSubject`, `decadeFrom/To`, `filmsOnly`) now in one interface that `ArchiveSearchParams` and `CatalogQuery` both extend; `sort` + query concept stay per-backend (`newest` is index-only, the live Solr path can't honor it); type-only, zero runtime change. (b) `docs/client-seam-design.md` — deep-module pass on app.js: names the deep modules (apiFetch, cardShell, renderGrid, renderError, renderResults, watchlist), the 5 hand-written fetch→render lifecycles, and the deepest refactor (`loadCatalogPage`) with the reason it's reported not done (touches every init on guarded code; needs option plumbing for real per-page differences). *Evidence: typecheck clean, 128/128, canonical smoke 220/220, deploy verified `environment = production`.*
- [x] **T6.35 Catalog-seam design pass (deploy #70 = f88bc817).**  Deep-module pass: extracted the variant→gate selection into one private helper `baseClauseFor(variant)` (was duplicated in `searchArchive` + `fetchCatalogIndexDocs`); behavior-neutral. New archive-unit test pins both gates at the archive seam (films union vs `collection:classic_tv` + license gate + `mediatype:movies`). *Evidence: typecheck clean, 128/128, canonical smoke 215/215, deploy verified `environment = production`.*
- [x] **T6.34 TV-boundary guards + browse films-field fix (deploy #69 = 40cf83fc).**  Adversarial review found the browse response body misidentified TV responses as films (`films: (films ?? true) || undefined` → `true` for `tv=1`); fixed to omit `films` on TV responses. Added 5 smoke guards: `tv=0` ≡ no flag (same total), films search for a TV-only term never returns classic-TV items (identifier disjointness), `tv=1` search returns TV results, films-search total sane, `tv=1` response omits `films`. Aligned `search.ts`/`app.js` comments with the shipped "Search TV shows" label. *Evidence: typecheck clean, 127/127, canonical smoke 220/220 against production, deploy verified `environment = production`.*
- [x] **T6.33 TV search (tv=1) + 1960s TV showcase (deploy #67 = 7f068f46).**  (a) **`/api/search?tv=1`** — search the classic-TV pool (same license gate); `validateQuery` gains `allowEmpty` (TV only) so the **Search TV shows shortcut** (`/search?tv=1`) lands on the pool newest-first instead of a 400; film search untouched (`tv` absent; `tv=banana&q=noir` → 400). (b) **1960s TV home showcase** — "The golden age of the tube" row (`tv=1&decade=1960&sort=newest`, 267 items, 1969 leads), see-all → `/browse?tv=1&decade=1960`. (c) **Search page honors tv=1** — "Classic TV · results for X", "shows" counts, pagination + form keep tv. (d) **KV re-verified as blocked on account access** (auth error 10000 — token lacks Workers KV perms); FOUNDER-CHECKLIST token steps made exact; code auto-activates KV when the binding exists. *Evidence: typecheck clean, 127/127 (allowEmpty test), dev smoke 215/215 (+6), browser render (1960s section 24 cards; Search TV link; both TV rows populate), API live (tv search 11 results; empty pool 2,514; films 394 untouched; 1960s 267), deploy verified `environment = production`, canonical smoke 215/215.*
- [x] **T6.32 Classic TV: the classic_tv collection under the same legality gate (deploy #66 = be165ca9).** (a) **TV variant of the catalog** — `TV_BASE_CLAUSE` (license gate + `collection:classic_tv` + mediatype:movies) in lib/archive.ts; lib/catalog-index.ts index is variant-aware (own in-isolate + edge caches per variant); `/api/browse?tv=1` serves it. **2,514 legal-marked items** (measured live); no year filter (1,180 lack year metadata — a filter would drop half the canon); films-only clause NOT applied (episodes ARE TV content). Quality caveat documented (classic_tv is looser than the film pools — a few modern shows carry self-declared marks; same trust boundary as the film gate, detail page fails closed). (b) **Front door**: "Classic TV" home section + /browse?tv=1 chip; decade/sort/q filters + filter line work on TV. (c) **Isolation**: films/sitemap/random untouched (separate index + cache keys; films still 15,917, films responses carry no tv field; `tv=banana` → 400). Warmup + smoke +8 (201 → 209). *Evidence: typecheck clean, 127/127 (TV-variant isolation test), dev smoke 209/209, API live (tv=1 → 2,514; decade=1960 → 267; films untouched; tv=banana 400),  browser render (home section 24 cards; /browse?tv=1 "Classic TV · Newest releases"), deploy verified `environment = production`, canonical smoke 209/209.*
- [x] **T6.31 Weekly new-arrivals report (scripts + workflow, no deploy).** `scripts/new-arrivals.ts` (`npm run new-arrivals`) queries archive.org with the exact catalog legality gate (licenseurl CC marks + curated collections + mediatype:movies + films-only clause, via `lib/archive.ts` `searchArchive`) for items added in the last N days and prints a markdown report (title/year/added date, site + archive.org links, day-grouped). Solr date-range gotcha fixed live (raw ISO colons/milliseconds rejected — quoted no-ms form works). `.github/workflows/weekly-new-arrivals.yml` cron Mondays 12:00 UTC (+ manual dispatch) opens a GitHub issue when ≥1 film landed (default token, `issues: write`, no new secrets); zero-arrival weeks log and open nothing. *Evidence: run live (90d = 107 films incl. the 2026 HK wave, day grouping + links correct; 7d = 0 — pools quiet), JSON/body-out path exercises the workflow's exact commands, YAML validated, typecheck clean, 126/126. No product code → no deploy.*
- [x] **T6.30 From-year filter + Newest releases as the browse default (deploy #65 = 71766afa).** (a) **`/browse` defaults to `sort=newest`** (release-year descending) — new arrivals lead on the bare URL; the sort is always sent explicitly so the UI default and API fetch can't diverge. (b) **"From year" select** (`id="from"`, 1920s…2020s onward) → `from=X&to=2020` (years X–2029) via the existing `validateDecadeRange`; **mutually exclusive with the decade select** (API rejects both; choosing one clears the other before navigating). (c) Smoke +3 (198 → 201): default-sort markup, from-filter presence, JS default-sort wiring. *Evidence: typecheck clean, 126/126, dev smoke 201/201, browser-verified (bare /browse = "All films · Newest releases" led by 2026; from=2020&to=2020 = 672 films, filter line "All films · 2020s onward · Newest releases"; UI selection navigates to the range URL), deploy verified `environment = production`, canonical smoke 201/201.*
- [x] **T6.29 Newest-releases sort, Hong Kong action home section, PD-collections RFC (deploy #64 = d361e381).** (a) **`sort=newest`** — release-year descending, missing years last, identifier tiebreak; the site's first release-year sort; browse UI option + URL round-trip + filter line. (b) **`q=` title-keyword filter** — ANY token ≥3 chars, case-insensitive substring, sanitized like search; **present-but-empty fails closed at 400** (draft treated `q=` as no-filter, contradicting the smoke guard — fixed in impl + unit test). (c) **"Hong Kong action" home section** (2nd): `q=dubbed+subtitled+kung+shaolin+wong&sort=recent&films=1` — 111 films led by the 2026 CC wave (Heroes Shed No Tears, Shaolin & Wu-Tang, High Risk Meltdown, New Dragon Gate Inn, Supercop); see-all → the keyword browse view. (d) **`docs/classic-films-rfc.md`** — the unmarked-PD collections question as a decision-only RFC (no code). Warmup + smoke extended (newest feed, keyword feed, fail-closed matrix). *Evidence: typecheck clean, 126/126, dev smoke 198/198, browser render of both new sections (24 cards each, HK led by the 2026 wave, clean console), API behavior verified live (newest tops at 2026; q= → 400 / sort=x → 400 / 81-char q → 400), deploy verified `environment = production`, canonical smoke 198/198.*
- [x] **T6.28 Modern picks home section + decade-range browse (deploy #63 = a13d868a).** Home now leads with a "Modern picks" section (years 2000–2029 via `from=2000&to=2020&sort=recent&films=1`) so the 2026 wave of CC uploads (HK-action restorations: Hard Boiled & The Killer, A Better Tomorrow, Police Story, Drunken Master…) gets front-door visibility instead of newly-uploaded old-film noise; new `/api/browse` `from`/`to` decade-range params (`validateDecadeRange` — decade starts from the same whitelist, both-or-neither, from ≤ to, mutually exclusive with `decade`; one-sided/reversed/conflict → 400); `initBrowse` deep links honor the range in the URL, filter summary, and pagination; warmup covers the feed URL. Smoke 183 → 191 (range CASES + 5 invalid-filter guards + home `#modern` section guard + JS feed guard). *Evidence: typecheck clean, 121/121 tests (validateDecadeRange + rateLimitConfig unit tests), dev smoke 191/191 (with the dev/CI `--binding RATE_LIMIT=10000` override — the smoke-vs-limiter boundary was root-caused and fixed: env-configurable limit, production default unchanged), canonical smoke 191/191, browser render of Modern picks (24 cards led by the 2026 wave, console clean), production live checks (home serves `id="modern"`; range feed = 2,264 films), deploy verified `environment = production`.*

- [x] **T6.27 Duplicate-id guard + CI workflow (PR #2, no deploy).** (a) **Smoke +1 (182 → 183):** the id guard now also fails any page repeating an id (breaks getElementById, ambiguates aria-* references); one fetch feeds both halves; negative-proven (doubled id in watchlist.html → exactly `DUPLICATES: /watchlist: zz-dup`, 182/183... the other failures in that run were the dev rate limiter, re-run clean). (b) **First CI: `.github/workflows/ci.yml`** — every PR + push to main: job 1 typecheck + 118 unit tests (Node 22, npm ci); job 2 live smoke against the PR's own code (wrangler pages dev :8788 → health wait → full suite with `SMOKE_MIN_SITEMAP_URLS=500` for CI speed); no secrets, no CI deploys; smoke-job sequence validated locally. *Evidence: dev smoke 183/183 (with and without CI env), node --check clean, no product code → no deploy.*
- [x] **T6.26 One card builder + dead-selector guard (deploy #62 = ba186d61).** (a) **`cardShell(item, saved)`** — the single definition of movie-card markup (grids, watchlist, search, browse, genre); `movieCard` normalizes the API shape, `watchCardHtml` passes saved=true; a missing title now defaults to "Untitled" (was escapeHtml(undefined) → literal "undefined" on hand-crafted storage). (b) **Dead-selector smoke guard (+1, 181 → 182):** every literal id app.js queries via $()/getElementById/querySelector(All) must exist on ≥1 served page (union of 7 pages — per-page would false-positive on page-specific ids); negative-proven (injected `$("#zz-never-shipped")` failed exactly that check, 181/182). (c) Stored-XSS guard string updated to the shared `escapeHtml(String(item.year))`. *Evidence: typecheck clean, 118/118, dev smoke 182/182, E2E 27/27 through the shared builder, browser render home+watchlist, deploy verified `environment = production`, canonical smoke 182/182.*
- [x] **T6.25 Tidy pass: dedupe browse/genre rendering, drop dead code, cut session scratchpads (deploy #61 = 6e7bddd4).** (a) **Dead code removed:** `initGenre`'s `#results-head` block could never run (genre.html has no such element) — deleted. (b) **~15 lines deduplicated:** count + grid + pagination rendering was verbatim-identical in `initBrowse` and `initGenre`; extracted `renderResults(grid, count, nav, data, makePageUrl)` (incl. the 100-page-cap honesty line), both call it; `initSearch` keeps its own (genuinely different) count line. (c) **smoke.mjs:** genre canonical check now reuses the existing `PINNED_ORIGIN` constant instead of a hardcoded URL. (d) **Removed session scratchpads** (task_plan.md/findings.md/progress.md — content fully covered by the project ledgers, recoverable from git). Nothing else in the thread's change was touched. *Evidence: typecheck clean, 118/118, dev smoke 181/181, browser /genre verified (138 films, pagination, clean console), deploy verified `environment = production`, canonical smoke 181/181.*
- [x] **T6.24 Version control: repo + initial commit + PR #1 (no deploy).** Project never had a git home (little-brother monorepo deliberately ignores it); with explicit authorization, created private https://github.com/mrfentmen/347movies — hardened .gitignore first (.freebuff/ session DB holds the deploy token, .venv-test/; secret-scanned, clean), docs/git-and-release.md, base main = .gitignore commit (ffa5562), production-ready = full site (c25da0a), **PR #1 OPEN MERGEABLE +15,453/-0, 101 files** (main←production-ready). Two GitHub API constraints worked around: PRs need an existing base branch and related histories (unborn default + two-root histories both rejected — resolved with the base commit + rebased child). *Evidence: typecheck clean + 118/118 re-run pre-commit; no deploy (shipped site unchanged).*
- [x] **T6.23 A11y coverage pass — /genre in axe+keyboard, 200% zoom audit, reduced-motion guard (no deploy).** axe-audit 10 → 11 pages (0 violations on all incl. /genre); keyboard walk adds /genre (72 tab stops ring-verified, 14 → 15 checks); mobile adds /genre + a 200% zoom section (640 CSS px viewport = rigorous headless 200%-zoom equivalent; setPageScaleFactor proved visual-only/no-reflow and was replaced — honest method note in changelog); smoke +1 reduced-motion guard (180 → 181). *Evidence: typecheck clean, 118/118, dev + canonical smoke 181/181, axe 0×11, keyboard 15/15, mobile 22/22. No product code → no deploy.*
- [x] **T6.22 No-accounts guard + watchlist export/import + accounts RFC (deploy #60 = c64f58a8).** (a) **No-accounts vow guard (smoke +10):** any served page shipping a password input / auth-route link / sign-up-sign-in text fails; prose denial passes; negative-proven (injected /login link fails only the /watchlist check). (b) **Watchlist export/import (server-free, +6 guards):** Export = Blob download of the localStorage list; Import = strict validation (fail closed), confirm-before-replace, role=status announcement; `.watch-utility` ghost pills; privacy page updated. E2E-verified (27/27: export .json download, import restores 2, status announces); keyboard 14/14 / mobile 15/15 / axe 0. (c) **RFC:** docs/accounts-rfc.md — the accounts decision space (what they'd buy, the §5/§9/Vow 5/specs amendments, D1+Better Auth cost, no-viewing-history red line) — recommendation: don't build accounts now. *Evidence: smoke 164 → 180, typecheck clean, 118/118, dev + canonical smoke 180/180, deploy verified `environment = production`.*
- [x] **T6.21 Better Auth review — declined (no code change).** The better-auth skill was activated; verified against constitution.md (§5 no accounts, §9 no auth unless the active phase calls for it, supremacy rule), vows.md (Vow 5 no accounts), and specs.md ("zero accounts", no auth phase, Phase 7 = launch active). Site is stateless and cookie-free (verified: no Set-Cookie / document.cookie in public/ functions/ lib/); Better Auth needs a database + session/cookie surface — a wholesale architecture change the constitution excludes. No install, no auth.ts, nothing to roll back. *If accounts are ever wanted: a constitution amendment by the owner first; the skill's content stays available for that decision.*
- [x] **T6.20 Genre landing page + design-system integrity guards (deploy #59 = 77438b56).** (a) **DESIGN.md → screen:** new `public/genre.html` (Film Noir destination — hero beam, eyebrow→Limelight rhythm, chips, skeleton grid, 300px sidebar ad slot) built entirely from DESIGN.md; wired via `app.js` `initGenre()` (count + grid + pagination, films=1), `.genre` two-column CSS (900px), `/genre` in sitemap staticPaths; browser-verified in the preview (hero, 138 films count, posters, clean console). (b) **Drift guard:** smoke design-system-integrity section — bidirectional DESIGN.md↔served-CSS hex parity (3-digit normalized), the rgba glow, and WCAG contrast on the 4 critical text pairs computed from live tokens; negative-proven both directions (tampered hex → both parity checks fail; darkened --muted → computed 2.40:1 contrast FAIL while others stay green). (c) **Contrast audit:** `scripts/contrast.mjs` + `docs/contrast-audit.md` — all 12 text pairs pass AA (worst 6.17:1; 9 pass AAA); the hairline non-text gap (1.07–1.37:1 vs 1.4.11's 3:1) documented with options, decision left to the owner. *Evidence: smoke 149 → 164 (+15), typecheck clean, 118/118 tests, dev + canonical smoke 164/164, deploy verified `environment = production`.*
- [x] **T6.19 DESIGN.md — semantic design-system source of truth (doc-only).** Synthesized `DESIGN.md` from the shipped `public/css/style.css` + `public/index.html`: atmosphere (the Projection Booth — cold-black walls, single tungsten accent), full palette with exact hex + roles, typography rules (Limelight display / Plex Sans reading / Plex Mono film-slate metadata), component stylings, layout principles (1120px cap, mobile-first 2→6-col grid, 300px detail sidebar). Every token cross-checked against live values; contract: stylesheet and doc must never drift. *No product code → no deploy. (design-md skill's Stitch MCP server unavailable; grounded in production CSS/HTML instead.)*
- [x] **T6.17 Hostile-input guard extension + KV enablement runbook (no deploy).** Follow-ups from the #58 pass: **(a) un-awaited-promise sweep** — every `return <call>` inside a try/catch in `functions/` + `lib/` audited; all remaining are synchronous (`jsonResponse`/`jsonError`/`toIndexedDocs`), the three `withEdgeCachedResponse` sites from #58 were the only instances of the bug class; no further fix needed. **(b) hostile-input guards extended** (smoke 129 → 140, 11 checks): encoded hostile identifiers (script/quote/null) → 400, invalid browse filters (genre/decade/sort/page) → 400, error responses carry noindex + nosniff — all verified live dev + canonical prod. **(c) KV runbook** — `LAUNCH-RUNBOOK.md` gets the one-pass enablement sequence with real verification (`wrangler kv namespace list` + `wrangler kv key get movie:<id>`) and rollback; blocker unchanged (Pages-scoped token, auth error 10000). *Evidence: typecheck clean, 118/118 tests, dev + canonical smoke 140/140, no deploy needed (no product code changed).*
- [x] **T6.16 Un-awaited withEdgeCachedResponse bug — search/browse upstream failures 500→502 (deploy #58 = da84ec51).** Adversarial verification (hostile-input probes from the T6.15 follow-up) found a real bug: a hostile search input (`a" OR 1=1 --`, sanitized to `a OR 1=1 --`) made archive.org's Solr reject the query, and the route returned **500 internal_error** instead of the intended **502 upstream_error**. Root cause: `functions/api/search.ts` + `browse.ts` did `return withEdgeCachedResponse(...)` **without `await`** — the rejection escaped the route's try/catch, making the `ArchiveError → 502` mapping dead code (every upstream failure became a middleware 500); `ad-config.ts` had the same un-awaited pattern, fixed for consistency. Fix: `return await` at all three sites. New `tests/search-route.test.ts` (3 regression tests; routes loaded via variable dynamic import to respect the test tsconfig's node-types boundary), suite 115 → 118. 6 new hostile-input smoke guards (smoke 122 → 129: Solr/traversal/script payloads sanitize to 200; upstream-rejected query → 502 honest upstream_error never 500; oversized + traversal identifiers → 400 pre-upstream). **Negative-proven both ways** (reverting the fix reproduces the exact 500 symptom and fails the guard). *Evidence: typecheck clean, 118/118 tests, dev+canonical smoke 129/129, deploy verified production, live repro returns 502.*
- [x] **T6.15 Security-header application research + 404 noindex hardening (deploy #57 = 75136642).** Researched (primary sources: Cloudflare Pages *Headers* + *Middleware* docs, then live probes) how `_headers` rules, the root middleware, and 404 handling interact — findings in `docs/cloudflare-headers-research.md`. Established: (1) `_headers` never applies to Pages Functions responses, so the middleware is the only header source for `/api/*`, `/movie/*`, `/sitemap.xml` — confirmed live and now smoke-guarded (function-route header set, 5 checks); (2) root middleware also wraps static files — both layers, no conflict; (3) the `/404.html` `_headers` rule is dead weight (the literal URL 308s to `/404`, unknown-route 404s never match it) — their noindex was meta-only. **Fix:** middleware now adds `X-Robots-Tag: noindex` to every 404 response (header-level, covers all origins; in-page meta stays as defense in depth), negative-proven. Smoke 115 → 122. *Evidence: typecheck clean, 115/115 tests, dev+canonical smoke 122/122, deploy verified production, live 404 carries the header.*
- [x] **T6.14 Accessibility follow-up pass (deploy #56 = b8c5b85f).** Executed the four follow-ups from the guidelines pass: **(a) focus-ring sweep** — found a real gap: the archive.org player embed is a cross-origin iframe whose focus never propagates `:focus-visible`/`:focus-within` to the parent (verified empirically: activeElement=iframe yet matches(':focus')=false), so keyboard users tabbing to the player saw NO ring; fixed with JS cross-origin focus tracking (focusout with `relatedTarget===null` + `activeElement` check → `.is-focused` on `.player-wrap`, cleared on focus return). **(b) axe-core live audit** — new `scripts/axe-audit.py` injects axe-core via the DevTools protocol (the strict CSP blocks script tags; protocol evaluation bypasses CSP without weakening it) — **0 violations** across 10 page states with the WCAG 2.0/2.1/2.2 A+AA tags. **(c) keyboard-only E2E** — new `scripts/e2e_keyboard.py` — 14/14: every Tab stop on every page (home 168, search 51, movie 17, browse 73, watchlist 15) lands visible with a real focus ring; found and fixed a second real bug — the skip link scrolled to `#main` but focus stayed on body (WCAG G1); `<main id="main" tabindex="-1">` on all 8 pages + SSR shell. **(d) notch/landscape audit** — new `scripts/e2e_mobile.py` — 15/15: 375px portrait + 667x375 landscape across 4 page types, no horizontal scroll; safe-area rule verified live (max(16px, env()) resolves; CDP can't fabricate env() in this Chrome — documented, real-device check needed for actual notch values); computed `color-scheme: dark` + theme-color verified. *All four suites wired as `npm run test:browser`; 4 new smoke guards (smoke 109 → 113). Evidence: 115/115 tests, dev+canonical smoke 113/113, E2E 24/24, keyboard 14/14, mobile 15/15, axe 0, npm audit 0, deploy verified production.*
- [x] **T7.3 Changelog entry.** Phases recorded with raw proof in `changelog.md`; `specs.md` status updated; this task list updated. Only KV/WAF caveats remain unverified — Lighthouse is done (see T3.5/T6.3).

---

## Post-launch additions (2026-08-15, recorded in `changelog.md`)

- **Watchlist** — Save/Saved toggles on every card and the SSR movie page; `/watchlist` page
  rendered from browser `localStorage` (privacy by default: no accounts, nothing sent to any
  server); privacy policy updated. *Browser-verified: save → toggle → clear flows all work.*
- **Full-catalog sitemap** — the sitemap now lists the entire legal catalog (18,495 URLs),
  not the first 5,000: Google can index every film and Surprise me is uniform over all of it.
- **JSON-LD structured data** — VideoObject on every SSR movie page (see T6.1) plus
  WebSite + SearchAction on the homepage (sitelinks searchbox), the schema shapes Google
  uses for video indexing and site search.
- **WCAG 2.2 accessibility pass** — error box announces via `role="alert"`; sticky-header
  focus not obscured (`scroll-margin`); ≥24px target sizes on nav/pagination/see-all links;
  `aria-current="page"` on active nav; card posters `alt=""` (decorative in the title link)
  with `data-title` keeping the initials fallback. Screen-reader tree verified in-browser.
  (Changelog deploy #32.)
- **`FOUNDER-CHECKLIST.md`** — exact dashboard steps for KV token, zone WAF, Search Console,
  ad network, affiliate tag.

---

## Definition of done

A task is done only when: (1) the work is real — no mock, placeholder, or pseudo code anywhere; (2) the acceptance criterion passed with raw proof; (3) no known errors remain; (4) the changelog records it. Blocked or unverified parts keep the task open and the blocker reported.
