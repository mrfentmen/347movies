node_modules/
.wrangler/
.dev.vars
.env
.env.*
dist/
*.log
.DS_Store

# Generated by scripts/register-images.mjs (npm run images) — transparency only; the
# committed artifacts are the --img-* block in style.css and the LCP preloads.
public/images/registry.json

# Session logs and tooling — .freebuff/ holds the agent session DB (desktop-v2.db
# contains the Cloudflare deploy token); .venv-test/ is the Playwright venv.
# These must NEVER be committed.
.freebuff/
.venv-test/
debug/

# Root-level photo attachments (the raw originals from the design handoff — the site
# serves the resized web copies under public/images/; the originals are never deployed).
/2000s-new-york-city-lower-manhattan-vintage-images.jpg
/2576805859_fcd5066eb3_b.jpg
/gettyimages-597-38-640x640.jpg
/zy21f38s9lqa1.jpg

# Scratch notes from working sessions (not site content; kept on disk for reference).
/findings.md
/progress.md
/task_plan.md
